{
  "name": "@polysync/sync-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Audio-correlation sync engine: GCC-PHAT alignment, drift estimation, project-wide solve. Zero dependencies, runs in Node and the browser.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
