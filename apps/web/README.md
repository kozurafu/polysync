# apps/web — NOT YET IMPLEMENTED (Phase 2)

Vite + React. Owns file picking, project state, the timeline UI, playback and
export. Orchestrates the ingest worker pool and the sync worker; contains no
DSP and no codec knowledge of its own.

See `docs/05-roadmap.md` Phase 2 for the acceptance criteria.
